# Upstream Lexbor Reference

Clone the C project here when porting behavior or tests:

```sh
git clone --depth 1 https://github.com/lexbor/lexbor.git upstream/lexbor
```

The checkout is intentionally ignored by Git. Ported PHP source and PHPUnit
tests should cite the upstream files they came from in comments or docs.

